<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!----======== CSS ======== -->
  <link rel="stylesheet" href="css/estiloForm.css">

  <!----===== Boxicons CSS ===== -->
  <link href='https://unpkg.com/boxicons@2.1.1/css/boxicons.min.css' rel='stylesheet'>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:400,400i,700,700i&display=swap">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css">


  <!--<title>Dashboard Sidebar Menu</title>-->
</head>

<body>
  <%- include('./barramenu') %>
  <div id="kt_content" class="content d-flex flex-column flex-column-fluid">
    <div class="innerMain_content">
      <div class="container innerMain_container">

        <div class="tab-content" id="myTabContent">
          <div class="tab-pane fade show active" id="list" role="tabpanel" aria-labelledby="list-tab">
            <div class="card">
              <div class="card-header d-flex justify-content-between align-items-center">
                <h1 class="page-title"><i class="bi bi-tools"></i> Lista Reservas de Activos Fijos</h1>
              </div>
              <div class="card-body">
                <div class="table-responsive">
                  <table id="userList" class="table table-bordered table-hover table-striped">
                    <thead class="thead-light">
                      <tr>
                        <th scope="col">ID</th>
                        <th scope="col">ID Activo Fijo</th>
                        <th scope="col">CI Persona</th>
                        <th scope="col">Fecha</th>
                        <th scope="col">Descripción</th>
                        <th scope="col">Acción</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% for (let i=0; i < reservas.length; i++) { %>
                      <tr>
                        <td scope="row">
                          <%= reservas[i].id %>
                        </td>
                        <td>
                          <%= reservas[i].idactivofijo %>
                        </td>
                        <td>
                          <%= reservas[i].cipersona %>
                        </td>
                        <td>
                          <%= new Date(reservas[i].fecha).toLocaleDateString() %>
                        </td>
                        <td>
                          <%= reservas[i].descripcion %>
                        </td>
                        <td>
                          <div class="table-cell">
                            <form action="/eliminarReserva" method="post">
                              <input type="hidden" name="id" value="<%= reservas[i].id %>">
                              <button>Eliminar</button>
                            </form>
                            <form action="/renderActualizarReserva" method="post">
                              <input type="hidden" name="id" value="<%= reservas[i].id %>">
                              <input type="hidden" name="idactivofijo" value="<%= reservas[i].idactivofijo %>">
                              <input type="hidden" name="cipersona" value="<%= reservas[i].cipersona %>">
                              <input type="hidden" name="descripcion" value="<%= reservas[i].descripcion %>">
                              <input type="hidden" name="fecha" value="<%= new Date(reservas[i].fecha).toLocaleDateString() %>">
                              <button>Actualizar</button>
                            </form>
                          </div>
                        </td>
                      </tr>
                      <% } %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>



      </div>
    </div>
  </div>
  </div>

</body>

</html>
